using UnityEngine;

public class MenuCameraProjectionPlane : MonoBehaviour
{
    void Start()
    {
        //idk if this is necessary
        transform.position = new Vector3 (-0.31f, 1.29f, -0.998f);
        transform.localScale = new Vector3(0.099f, 0.22f, 0.07f);

    }
}
